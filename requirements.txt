# Runtime for Vercel — без CBC/HiGHS (тяжёлые бинарники).
# На serverless при отсутствии solver сработает greedy fallback.
fastapi>=0.110.0
pandas>=2.1.0,<3
numpy>=1.26.0,<3
pydantic>=2.5.0
python-multipart>=0.0.6
openpyxl>=3.1.0
pulp>=2.8.0,<3
